/*
import scala.collection.mutable.ListBuffer

/**
 * Created by LalinPethiyagoda on 03/06/2016.
 */
object FlattenList extends App{
  val aList = List(1,2,3,5,6,6,8)
  val bList=List(3,4,5)
  val cList= List(6,7,8)
  val totList = List(aList,bList,cList)

  def func(lst:List[Int]):List[Int]=lst match{

    case Nil=>Nil
    case head::tail=>{
      var listBuffer = new ListBuffer[String]()
      var x=head
      var counter =0

      }
    }


  }
def flattenList(ls:List[Int]):Unit= {
  ls.map(x=>x.equals(x)).flatten
}
}
*/
